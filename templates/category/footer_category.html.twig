<h5>Catégories</h5>
<ul class="list-unstyled text-small">
    {% for category in categories %}
        <li><a class="text-muted" href="index.html.twig">{{ category.label }}</a></li>
    {% endfor %}
</ul>